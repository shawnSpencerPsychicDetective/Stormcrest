[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "stormcrest"
version = "0.1.0"

[tool.setuptools.packages.find]
where = ["."]

[project.scripts]
stormcrest = "stormcrest.__main__:main"
